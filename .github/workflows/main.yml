name: Weekly Data Ingest

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Run without writing data"
        required: false
        default: "true"
      branch:
        description: "Branch to run the workflow on"
        required: true
        default: "git-action-fix"

  schedule:
    - cron: "0 0 * * 1"

jobs:
  ingest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout selected branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch || github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set date range
        run: |
          END_DATE=$(date -u -d "yesterday" +%F)
          START_DATE=$(date -u -d "$END_DATE -6 days" +%F)
          echo "START_DATE=$START_DATE" >> $GITHUB_ENV
          echo "END_DATE=$END_DATE" >> $GITHUB_ENV

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run ingest job
        env:
          APP_TOKEN_API_KEY: ${{ secrets.APP_TOKEN_API_KEY }}
        run: |
          python scripts/pull_rodent_data.py \
            --start-date=$START_DATE \
            --end-date=$END_DATE \
            --dry-run=${{ github.event.inputs.dry_run || 'false' }}

      - name: Run data processing
        env:
          GOOGLE_PLACES_API_KEY: ${{ secrets.GOOGLE_PLACES_API_KEY }}
        run: |
          python scripts/pull_nearby_locations.py \
            --dry-run=${{ github.event.inputs.dry_run || 'false' }}
